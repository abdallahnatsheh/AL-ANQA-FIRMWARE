# Memory Index

## Rules (always apply)
- [Collaboration + Coding Rules](feedback_rules.md) — no AskUserQuestion, reuse code, verify APIs, GDMA critical rule, user profile
- [User compiles manually](feedback_user_compiles_manually.md) — do NOT run pio builds; static-review then hand off; CI is the gate
- [UI Rules](ui_rules.md) — `[CYAN::YELLOW] 01/02` header style + cursor corruption fix pattern
- [NimBLE v2.x Rules](nimble_v2_rules.md) — scan response name, cleanup no re-init, scan non-blocking, auto-bond-delete

## State
- [Progress Log](progress_log.md) — **2026-07-09c: wpa3down Phase 2 FIRST-HW-TESTED vs a phone hotspot — attack works mechanically, CAPTURE blocked by client PMF (documented limit, not a bug).** `w3d` rogue WPA2 AP beacons + victim probes it, but the modern Linux laptop victim negotiates PMF (AP is MFP-capable) → forged deauths rejected → won't drop. Added directed deauth (`w3d [idx] [victim-mac]`) + continuous flood (~50/s) + compile fix (`extern sdCardManager`). **▶ RESUME: prove capture with a NON-PMF victim (old phone/IoT/ESP32); phone-hotspot BSSID is stable-while-up (re-scan sw right before, don't toggle); victim laptop MAC `98:bd:80:b4:0d:52`, target `LABs` ch6.** All UNCOMMITTED. Full detail in [[project_wpa3down_plan]] HW-TEST FINDINGS. Prior **2026-07-09b: wpa3down Phase 2 BUILT** (TD picker → karma `roguehs` WPA2-only rogue AP + deauth → M2 → `/apps/wpa3down/<ssid>.cap`, crack w/ `cc`). Prior **2026-07-09: `sw` reworked into a WiFi MANAGER — code-complete, UNCOMMITTED, NOT HW-tested.** `sw on`/`sw off` radio power + bare `sw` = interactive manager: status line (current conn) + trackball-selectable scan list, CLICK/Enter=connect, `[d]`isc `[f]`orget (NVS + wpa_supplicant block) `[o]`n/off `[u]` `[q]`; keeps the scan-index contract (cw/da/hs/ws… unaffected), reuses `connectToWiFiCommand`, off=STA-idle (never WIFI_OFF), shared `drawNetRowCells` (SEC incl TD + WPS). NEXT = flash+test, then wpa3down Phase 2. Prior **2026-07-08: home launcher → REAL working apps + AA polish + OOP refactor — ✅ HW-VERIFIED, UNCOMMITTED (ready to commit).** 7 working apps (Calculator/Clock stopwatch+timer/Reminders SD-persisted/Weather/Calendar/Flashlight/Settings→Wi-Fi two-page), alarms via `NotificationManager` (friendly SUCCESS/INFO chimes, ring 5× till dismissed, `notify(lvl,force,allowCovert)` — allowCovert is the ONLY covert-sound exception). Notes search made functional + trackball back-chevron nav. Fixed: **wguard/EC/MW badges exposed the cover** (now `isBlocked()`-guarded in display_manager), cover signal icon = real RSSI, **WIFI_OFF GDMA violation** in Settings → `disconnect(false)`. **Refactor #1 DONE: shared `cover_kit.{h,cpp}`** (sprite+fonts+passphrase+touch-wake, home+notes share via `G` aliases). **OOP refactor DONE** (State/Strategy + DI + Facade): `cover_kit`(shell) · `home_app.h`(HomeApp base) · `home_widgets`(`Ui` widget manager) · `app_*.{h,cpp}` (7 apps, ONE class per file) · `home_ui.cpp` = `HomeLauncher` orchestrator (~325 lines, was ~1580). **Whole refactor COMPILES CLEAN; bonus −44KB flash from the dedup.** NEXT: HW test + commit. Feedback: never say "god"-object/file ([[feedback_rules]] 8b). Prior **2026-07-07b: home-launcher undercover UI BUILT (`home`/`hm` [EXP]).** BlackBerry-style dark home screen (status bar + clock/weather hero + 4×2 app grid, vector-primitive icons) — only the Notes tile is real (→ `runNotesUi(false)`), others show "No service". Reuses ALL Notes cover plumbing (PSRAM sprite, baked fonts, setBlocked, dim/wake, lock stand-down, secret-passphrase exit). Refactored `runNotesUi()`→`bool runNotesUi(bool standalone=true)` (backward-compat) so Home retakes the screen flash-free on a normal back but propagates a passphrase exit to CLI. Files `core/system/undercover/home_ui.{cpp,h}`. **Command cap raised 64→128** (`Command commands[128]`). Also 07b: **real battery** in notes+home status bars (`batteryManager.getPct()`, 10s cache), **real weather** in the home hero — new `WeatherManager` + `weather`/`wx` (OpenWeather HTTP+ArduinoJson7, GPS/config location, self-seeding `/config/weather.conf`, GDMA-safe fetch only on `wx now`/home-entry). Docs added everywhere: man (`home`/`weather` pages), docs/ (home.md+weather.md+system.md), README, CLAUDE.md. **HOME is now the undercover BASE** (`runUndercover`/boot-cover call `runHomeUi()`); **standalone `notes`/`nt` command REMOVED** (Notes only via the home tile; `runNotesUi` kept). **Charging bolt** added to the home battery. Notes list has a **back-to-Home chevron** (+`[q]`). **64/128 cmds.** Full detail [[project_undercover_home_launcher]]. Prior **2026-07-07: isoscan back-stack nav + CLI bad-arg DRY + bs-android reboot fix (✅ HW-TESTED, COMMITTED + PUSHED).** isoscan `[q]` now steps back attack→menu→picker→CLI (attack fns `void`→`bool`: true=menu, false=exit); every SD-writing attack shows its file; mitm pcap renamed `mitm_NNN.pcap` (distinct from portdown), no overwrite anywhere. **CLI bad-arg rejection is DRY:** new `Utils::printUsage(cmd)` prints the command table's registered `description` (single source of truth) instead of hand-copied usage; `Utils::checkChannelArg` fixes `wm ff` (atoi→0 silently ran). Per-handler NOT central (a kArgHints validator would break `bs` synonyms ios/windows/galaxy); migrated wp/show/tm/jg/mc/mw/km/ns/csi/gps/bs/fp. **`bs android` reboot fixed:** pre-existing per-cycle `deinit(true)+init()` MAC-rotate → NimBLE-native `setOwnAddr` (like `mc`, see [[nimble_v2_rules]]). lockscreen `g_covert` intercept guard committed with batch (not authored this session). Prior **2026-07-06b: isoscan interactive-UI flicker/clarity rework (now COMMITTED)** — killed the ~50fps strobe (was `lastDraw=0` in the 20ms TX tick); menu shows per-attack description + CLI `[keyword]`; inject/gwpoison value-in-place; plain-language line per screen; `dns` live last-5 query list. **`dns` dual-stack UDP:53 listener fix** (WiFiUDP was IPv4-only → raw lwip `IPADDR_TYPE_ANY`) — but **`dns` is IPv6-ONLY & confirmed DEAD on IPv4-only nets/phone hotspots** (victim had only fe80:: link-local, IPv4 DNS). Prior **2026-07-06: isoscan/is DONE + HW-TESTED + HONEST + docs.** Built L1 smart-`auto` (6-stage probe→verdict+recommend) + L2 `mitm` (poison+capture at once), then made the whole tool HONEST after HW showed the MITM over-claiming. Fixed the false "MITM LIVE" (was counting the victim's ARP *reply* as data-redirect) via ethertype classification (`s_capArpAck`/`s_capRedir`/`s_capCand`) + **rate-gating** (LIVE only on sustained ≥8/s). `bounce` reworked **ARP-first** (works vs Windows firewall; HW✅). Menu reordered auto-first + honest `[exp]` tags. **SETTLED via AirSnitch/Bruce/Marauder research: real traffic MITM is NOT achievable on the single-radio T-Deck** — AirSnitch interception = port-stealing on a 2nd BSSID = needs 2 radios. `inject` (isolation-bypass, HW-proven) is the novel win (Bruce/Marauder have nothing like it). **isoscan = honest isolation-AUDIT + recon + inject tool**, NOT an interceptor; `mitm/portup/dns` `[exp]` report truthfully "not held". Docs all updated (man/README/CLAUDE/network.md + new docs/isoscan.md). UNPUSHED. **▶ OPTIONAL NEXT: port-stealing spike (2nd-BSSID MAC-spoof) — only path to real interception, half-duplex on 1 radio; spike before promising. Else isoscan complete.** Prior **2026-07-04: undercover PANIC BUTTON ✅** (`uc panic`, `panic_key` `@`, instant-hide mid-command). Undercover feature-complete. Decoy/duress + freeze-TX = user DROPPED. Prior: mc/NimBLE spoof fix (89765e0/6a50e50)

## Done
- [Undercover mode + touchscreen](PLAN-undercover-touch.md) — Phase 0 ✅ Phase 1a ✅ Phase 2 ✅ boot-cover ✅ **panic-button ✅ (2026-07-04)** — ALL HW-verified. Touch, Notes cover UI, secret-passphrase exit, SD-backed notes + cursor editor, boot-cover, and instant-hide `panic_key` (default `@`, re-entrant hook in getKeyboardInput, cover-exit `signalRedraw()` repaints mid-command apps). **Decoy/duress passphrase + freeze-transmitters ops-policy = user DROPPED as not useful.** Feature-complete for the user. Optional polish only (ux/portal-picker repaint). Full detail: progress_log 2026-07-04.
- [Next Steps](next_steps.md) — priority queue #1-24 (20 unbuilt: ARP/LAN-MITM/w3d/Responder/TCP/DPWO/BadBLE/BTrelay/GPStrack/USB×4/VNC/QR/LoRa-scan/Chromecast/mic-WAV/NES) **+ 2026-07-07 brainstorm #25-43 (verified-unbuilt: LoRa/Meshtastic⭐, Pwnagotchi, WPS, hot-cold locator, iBeacon bcast, GATT fuzzer, audio spectrum/tuner, TOTP⭐, games, WoL/MQTT/curl, add-on IR/NFC/433).** `lt` command note was stale (LoRa radio unused; only `test lora`). Cap `commands[64]` is arbitrary — raise to 128 (~2KB) to work the list.

## Improvement backlog (hardening — user will consider)
- [Improvement backlog](project_improvement_backlog.md) — project-wide hardening: #1 unit tests for crypto/parsers, #2 validate compiled-but-untested features, #3 extract dot11_tx/promisc shared utils, #4 finish ScopedPromiscPause migration, #5 64-cmd cap (at 59 — freed 5 via merges, added edit+macwatch+csidetect), #6 central WiFi state. NOT new features.

## Open Issues (verify / fix later)
- [ESPVoice crash watch](project_espvoice_crash_watch.md) — `ev` sometimes crashes after a couple min; PS_NONE + draw-throttle applied (unconfirmed); read RESET REASON to classify (brownout vs panic vs WDT)
- [Bluedroid/BLE-Mesh RAM+flash leak](project_bluedroid_ram_flash_leak.md) — **FIXED + HW-VERIFIED 2026-07-02**: `mac_changer.cpp` swapped legacy Bluedroid GAP calls for NimBLE-native (`setOwnAddrType`/`setOwnAddr`); saved 19.7KB RAM + 428KB flash on T-Deck-Plus. User confirmed `mc random`/`mc status` now actually changes the BLE MAC on hardware (old code was dead — Bluedroid never enabled elsewhere, so `mc target bt` silently no-op'd before). Note: `bk`/`bd` hardcode their own stable BLE address and can't be used to test this over the air (see file for why).

## Planned features (NOT YET BUILT — design specs captured 2026-06-26; full plans in TREX_*_PLAN.md)
- [usblan / adblock](TREX_USBLAN_PLAN.md) — **SHELVED 2026-07-12 (too complex vs payoff), full research captured.** T-Deck as a USB-NCM Ethernet gadget bridging PC↔WiFi with an SD-sourced DNS sinkhole / DNS-MITM. User wants the USB form specifically (knows the WiFi ARP/DNS-spoof alternative). **Verdict: silicon YES, current Arduino stack NO** — NAT (`ip_napt_enable`) won't link in arduino-esp32's precompiled libs (#6421/#8193) + TinyUSB NCM not exposed; both = frozen compile-time config, can't be fixed with a hand-added .c (core-path call sites frozen off). USB dongle intrinsically needs NAT (all PC traffic forwarded). **Only way to a native command = one-time `esp32-arduino-lib-builder` rebuild** (same Arduino-2.0.x/IDF-4.4 versions + NAPT/NCM flags on + `leeebo/tinyusb_src`); else a **standalone dongle firmware** (near-ready IDF-4.4 base: ThingPulse esp32-pendrive-s3-wifi-dongle / esp-iot-solution usb_dongle already do WiFi-internet-over-USB). Design: no web UI (native screen), blocklist on SD → ~1.6MB PSRAM sorted-hash index. Verified `tinyusb_net_*` API + full route analysis in the plan.
- [Undercover home launcher](project_undercover_home_launcher.md) — **LOW PRIORITY** polish idea: BlackBerry-style home-launcher undercover UI (status bar + clock/weather hero + 4×2 app grid, fits 320×240), reuses the Notes cover sprite/touch/passphrase plumbing. Behind isoscan smart/auto + active testing.
- [wpa3down/w3d](project_wpa3down_plan.md) — WPA3 transition-mode downgrade. **⏳ PHASE 1 DONE (2026-07-08, HW-test pending):** `sw`/`show` now tag security per net (TD=yellow=transition/downgradeable, WPA3=green, etc.) via `wifi_ap_record_t.authmode` → `NetworkEntry.sec`/`WifiSec`/`classifySec`/`getNetworkSec` in wifi_functions; no TX. NEXT = Phase 2 attack (PMF probe→WPA2-only rogue AP→ws/pm capture), Phase 3 pre-assoc flood, Phase 4 wguard downgrade-detect. Full: TREX_WPA3_DOWNGRADE_PLAN.md
- [netspy/ns + isoscan/is](project_netspy_isoscan_plan.md) — Stage 0+1+**1b ✅DONE & HW-verified** (committed, unpushed). `ns` = PASSIVE client-isolation recon (AirSnitch): sniffs AP-relayed group frames (assoc'd ESP32 HW decrypts them → no CCMP needed), parses ARP/IPv4/DHCP/mDNS/SSDP → IP+name+services; Enter/`[i]` detail; `[p]`/`[o]` + `ps ns#`/`pg ns#` probe discovered devices; GTK at `gWpaSm+0x174` (platform pinned `espressif32@7.0.1`). **NEXT = Stage 2: active `isoscan`/`is` GTK-inject** (separate opt-in command, transmits). **Victim-targeting design captured 2026-07-05** (reuse netspy `#` index/`ns#` prefix + `s_dev[]`; export MAC+name from netspy.h; CLI `is ns#` + in-app picker + confirm-before-fire; resolve gateway MAC for `bounce`). See this file's BUILD STATUS + "Stage 2 VICTIM TARGETING design".
- [csidetect/csi](project_csi_camdetect_plan.md) — ✅ BUILT + HW-VERIFIED 2026-06-26; PRO **WiFi motion detector** `[EXP]` (sprite ~30fps + 8 subcarrier-band sectors). **Honestly NOT a radar** (single antenna = motion energy, no bearing). Next: SD presence log/alert. camdetect DROPPED. Full: TREX_CSI_CAMDETECT_PLAN.md

## Feature References (look up when touching that feature)
- [BlueToolkit BLE ideas](bluetoolkit_ble_ideas.md) — from sgxgsx/BlueToolkit research (2026-07-12): what's implementable on the T-Deck (BLE-only). Usable = **`gattfuzz`** (GATT char fuzz/abuse, HIGH, reuse `bleinfo`) + **`bleaudit`** (BLE security auditor) + optional BLE-DoS. NOT usable = all BT-Classic attacks (no BR/EDR radio) + SweynTooth LL fuzzing (no raw link-layer). IDEAS ONLY — CC BY-NC-SA license, no code reuse.
- [Karma/km](project_karma_plan.md) — NOT YET BUILT; design+build plan: Auto/Interactive modes, portal+WPA2-handshake bait, PNL fingerprinting (defeats MAC rand), intel cards, reactive karma
- [SSH client/ssh](project_ssh_client.md) — LibSSH-ESP32, 50KB task, colour terminal+scrollback; HW-SHA concurrency caveat
- [buddy/bd](project_buddy_port.md) — working; key NimBLE quirks
- [BLE Info/bi](project_bleinfo.md) — working; critical compile quirks
- [WGuard/wg](project_wguard_feature.md) — full IDS threat table, detection logic, save types
- [Beacon Flood/bf](project_beacon_flood.md) — 5 modes, GDMA-safe, dynamic frame builder
- [Lock screen blocking](project_lock_display_blocking.md) — setBlocked/consumeJustUnlocked per-command table
- [GPS warm start](project_gps_warmstart.md) — L76K/M10Q NVS cache, init commands
- [NotificationManager](project_notification_manager.md) — NOT YET BUILT; I2S WAV spec
- [USB Gadget](project_usb_gadget_plan.md) — MSC+HID; key SPI fixes
- [macwatch/mw](project_macwatch_idea.md) — BUILT 2026-06-25, NOT yet HW-tested; "trackme-lite" dual-radio loop, presence state machine, OUI-prefix matching, hunt meter, BLE-only bg mode, reuse map, GDMA-safe gap. Also `ble_ident.h` (SIG/Continuity/AirPods device ID, used by bmon/sbl/mw)
- [Remote CLI / screen mirror](project_remote_cli_screen_mirror.md) — NOT YET BUILT; USB-CDC text mirror of DisplayManager, Flipper-CLI style
- [Future peripherals](project_future_peripherals.md) — ES7210 mic (I2S_NUM_1, both boards), GT911 touch pins
- [espvoice/ev + mictest/mt](progress_log.md) — ESP-NOW G.722 walkie-talkie + mic test; PTT toggle, coexist-resident I2S, libg722 vendored, app-local vol/gain (see 2026-06-12 in progress log)
